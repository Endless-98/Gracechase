# Frontend Environment Variables
# Copy this file to .env (or .env.local) and fill in your actual values

# Google Analytics 4 Measurement ID (optional)
# Get this from your GA4 property settings
VITE_GA_MEASUREMENT_ID=GA-XXXXXXXXXX

# Backend base URL used by the frontend to call APIs
# Development: http://localhost:5000
# Production: Replace with your Railway/Render/other deployment URL
VITE_BACKEND_BASE_URL=http://localhost:5000

# Cloudflare Turnstile site key (public)
# Required for newsletter signup bot protection in the UI
VITE_TURNSTILE_SITEKEY=

# Feature flag to enable sending confirmation emails (keep false until SES production access)
VITE_SEND_CONFIRM_EMAIL=false

# Legacy/unused variables (kept for compatibility if referenced elsewhere)
VITE_API_URL=http://localhost:5000
VITE_NEWSLETTER_CONFIRM_URL=